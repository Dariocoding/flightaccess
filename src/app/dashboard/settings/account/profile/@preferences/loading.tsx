import * as React from 'react';

import { PreferencesSkeletonCard } from '@/app/dashboard/settings/components/account/profile/preferences-skeleton-card';

export default function PreferencesLoading(): React.JSX.Element {
  return <PreferencesSkeletonCard />;
}
