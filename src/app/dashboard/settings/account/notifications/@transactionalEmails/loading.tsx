import * as React from 'react';

import { TransactionalEmailsSkeletonCard } from '@/app/dashboard/settings/components/account/notifications/transactional-emails-skeleton-card';

export default function TransactionalEmailsLoading(): React.JSX.Element {
  return <TransactionalEmailsSkeletonCard />;
}
