import * as React from 'react';

import { SessionsSkeletonCard } from '@/app/dashboard/settings/components/account/security/sessions-skeleton-card';

export default function ManageSessionsLoading(): React.JSX.Element {
  return <SessionsSkeletonCard />;
}
