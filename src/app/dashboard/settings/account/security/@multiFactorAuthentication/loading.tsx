import * as React from 'react';

import { MultiFactorAuthenticationSkeletonCard } from '@/app/dashboard/settings/components/account/security/multi-factor-authentication-skeleton-card';

export default function MultiFactorAuthenticationLoading(): React.JSX.Element {
  return <MultiFactorAuthenticationSkeletonCard />;
}
