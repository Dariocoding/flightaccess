import * as React from 'react';

import { ChangePasswordSkeletonCard } from '@/app/dashboard/settings/components/account/security/change-password-skeleton-card';

export default function ChangePasswordLoading(): React.JSX.Element {
  return <ChangePasswordSkeletonCard />;
}
