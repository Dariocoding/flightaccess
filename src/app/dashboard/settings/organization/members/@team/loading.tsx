import * as React from 'react';

import { MembersSkeletonCard } from '@/app/dashboard/settings/components/organization/members/members-skeleton-card';

export default function TeamLoading(): React.JSX.Element {
  return <MembersSkeletonCard />;
}
